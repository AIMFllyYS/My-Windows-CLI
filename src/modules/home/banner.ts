import chalk from 'chalk';

const LOGO_LINES = [
  '  ██████╗        ██╗       ██████╗██╗     ██╗',
  ' ██╔═████╗      ███║      ██╔════╝██║     ██║',
  ' ██║██╔██║█████╗╚██║█████╗██║     ██║     ██║',
  ' ████╔╝██║╚════╝ ██║╚════╝██║     ██║     ██║',
  ' ╚██████╔╝       ██║      ╚██████╗███████╗██║',
  '  ╚═════╝        ╚═╝       ╚═════╝╚══════╝╚═╝',
];

export function renderHomeHeader(version: string): string {
  const logo = LOGO_LINES.map((line) => chalk.cyanBright(line)).join('\n');
  const title = chalk.bold.white(`0-1 CLI v${version}`);
  const quote = chalk.yellow('树林曾云：从0到1是最贵的');
  const subtitle = chalk.gray('希望这个CLI可以帮助你从0到1入门AI-CLI工具');

  return `\n${logo}\n\n${title}\n${quote}\n${subtitle}\n`;
}